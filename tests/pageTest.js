var should = require('should');

var define = require("../libs/require");
/*
requirejs.config({
    baseUrl: '.',
    nodeRequire: require
});

//var car = require("../src/page.js");
var assert = require("assert")

define(function(require){

    describe("Page", function() {
        var Page = require("../src/page.js");

        describe('#1 page:', function(){
            
            var examples = new Page();

            it('should exist', function() {
                expect(examples).to.exist;
            });
     
            it('should be an instance of Examples collection', function() {
                expect(examples).to.be.an.instanceof(Page);
            });

            it('Other.test', function(){
                examples.getLoaded().should.equal(true);
            });
        });
    });
});*/

